{% extends 'base.html.twig' %}

{% block title %}Hello WeatherController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>{{location.city}}, {{location.country}}</h1>
    {{ "[ %s, %s]"|format(location.latitude, location.longitude) }}

    <ul>
        {% for data in weatherData %}
            <li>{{ data.temperature }}&deg;C</li>
        {% endfor %}
    </ul>

    {# <h1>Hello {{ location.city, location.country }}</h1> #}

    {# <ul>
    {% for data in weatherData %}
    <li>{{ data.date|date('d.m.Y') }}: {{ data.celsius }}&deg;C</li>
    {% endfor %}
 </ul> #}

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'E:/laragon-portable/www/AI2-weather-app/src/Controller/WeatherController.php'|file_link(0) }}">src/Controller/WeatherController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'E:/laragon-portable/www/AI2-weather-app/templates/weather/index.html.twig'|file_link(0) }}">templates/weather/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
